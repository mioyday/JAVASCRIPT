// 증가 & 감소 연산자 Increment & Decrement Operators
let a = o;
console.log(a);
a++; //a = a + 1;
a--; //a = a - 1

console.clear();
// 주의!
// a++ 필요한 연산을 하고, 그 뒤 값을 증가시킴
// ++a 값을 먼저 증가하고, 필요한 연산을 함

a = 0;
let b = a++;
consonle.log(b); //a=1 b=0

a = 0;
let c = ++a; // c = 1 a = 1
consonle.log(c);

